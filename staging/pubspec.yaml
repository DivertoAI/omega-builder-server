name: hello_button_app
description: A minimal single-screen Flutter app that displays 'Hello' text and a button.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
